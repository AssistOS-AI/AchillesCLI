# Launch Codex

Delegate a natural-language task to the agent `codexAgent`.
Use when Codex or codexAgent is explicitly requested to execute a task in the current project.

## Input Format
Accepts only plain text describing the task to execute.

## Output Format
Returns plain text `Task started.` for asynchronous execution or a completed result for blocking execution; treat both as success.
